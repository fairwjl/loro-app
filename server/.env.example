# server/.env.example
# OpenAI API key (replace with your own key in .env)
OPENAI_API_KEY=your-openai-key-here
# Server port
PORT=8787
# Optional: database connection string (if/when added)
# DATABASE_URL=postgres://user:password@localhost:5432/mydb

